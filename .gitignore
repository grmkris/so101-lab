sim/.venv/
sim/model/assets/
__pycache__/
*.local
*.blend1
chess_system/.local/

# machine-local agent/MCP tooling — the app itself lives in app/ (bun);
# the root npm tree exists only to back the shadcn MCP server
/node_modules/
/package.json
/package-lock.json
/.mcp.json
/.claude/
/.agents/
/skills-lock.json
/.agent-sources/

# regenerable memoization cache — churns every run, grows unboundedly during a game
chess_system/mujoco/generated/runtime_trajectory_cache.json
