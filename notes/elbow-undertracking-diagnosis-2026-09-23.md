# Elbow under-tracking diagnosis — 2026-09-23

This is a diagnosis from saved commissioning ledgers, deployed-source snapshots and offline tests. No motion, contact trial, gain/limit/calibration write, completion-guard change or provider call was made during this diagnosis. The arm remains stationary while physical camera repositioning is awaiting Kris's confirmation.

## Evidence

- The isolated elbow-only request changed the target by 1.6 degrees over 1.142857 seconds. It moved 0.7912088 degrees and failed at a residual of 0.8087912 degrees (`ea3781a9-7a13-4d0e-9387-088a2aa02a76`). The target was 54.356044 degrees and the measured elbow was 55.164835 degrees.
- Fresh read-only snapshots at 09:46 and 10:25 CEST had the same measured joints, commanded targets and operation ID as the 08:57 failed move. At 10:25 there was no fault, operator or running chat, the boot ID was unchanged, cameras were fresh (51/25 ms), and the maximum servo temperature was 42 C. These are separate samples, not continuous proof of a plateau. The matching elbow reading about 89 minutes later provides no evidence that a longer timeout will fix it. The engine keeps commanding the failed target after its duration plus two-second settling window expires.
- The deployed conversion uses 4095 counts per revolution. It predicts raw goal 2642 (54.285714 degrees), while the measured position corresponds to 2652. Truncation shifts the goal 0.07033 degrees farther in the requested direction; the expected hardware residual is 0.87912 degrees (ten ticks), larger than the reported 0.80879 degree residual. Encoder truncation cannot account for the entire miss if the predicted goal reached the servo. The actual `Goal_Position` register has not been read back.
- Historical loaded traces show pose and direction dependence: on 2026-09-17 P32 moved 0.000 degrees, P48 left 0.833 degrees and P64 left 0.393 degrees; on 2026-09-18, a different working pose had P96 residuals of 0.042 degrees outward and 0.264 degrees returning. These are different runs and are not a controlled comparison with today's failure. The saved deployed profile specifies elbow P96, max step 2 degrees and max speed 2 degrees/second; actual current P/I/D registers have not been read back.
- The checkout and saved deployed engine are AST-equivalent. The offline saved-evidence test file passed 6 tests again at 10:18; the earlier selected engine safety run passed 5 tests. These cover measured terminal snapshots, held-joint judging, stale-camera cancellation, timeout cleanup, conversion rounding and replay of the recorded endpoints. The replay uses a fake driver: it validates software behavior, not servo physics. The ordinary observations lack raw current/load/voltage, so they cannot establish the physical cause.
- A new offline command-path audit reconstructed the checkout and saved deployed engine at 10 ms, 33.3 ms and 100 ms synthetic ticks. All six runs agree: the operation starts from measured elbow 55.956044°, while the prior commanded hold was 55.046°. The first generated commands are 55.942°, 55.909° and 55.816° respectively, so the trajectory initially moves back toward the measured pose before crossing the old goal after 0.66–0.70 s and reaching the final encoded target after 1.15–1.20 s. The endpoint replay fails at the same residual with no engine fault and leaves other joints unchanged. This is a command-path hypothesis, not a physical transient recording; private output is `robo-harness/var/elbow-diagnosis-2026-09-23/command-path-audit.json`.

## Decision and next diagnostic

Do not change P96, limits, calibration or the 0.8 degree completion guard. Do not repeat the failed motion or run the historical `servo_step_trace.py` unchanged: it is a sole-owner trace that writes P and restores a goal in `finally`, so it is not a passive readback.

The next diagnostic is a **zero-motion raw snapshot and short passive trace of the existing hold**, not another motion. Record raw goal and present position, P/I/D, operating mode, torque enable, current, load, voltage, temperature, moving/status, boot ID, monotonic and wall clocks, and read latency. Preserve raw values as well as their decoded units. Read goal before and after the sample set to expose a change during a non-atomic snapshot. Capture every joint's ordinary position alongside the elbow registers.

There is a concrete implementation prerequisite: today's API has no raw-register route. `LeRobotDriver.read()` returns positions; `temperatures()` adds temperature. Adding a diagnostic must use the existing motor owner's serialized bus access and expose a cached result. Slow bus reads need a bounded sampling budget against the 250 ms control deadline; disk/network work stays off the motor loop. Missing fields or read failures must stay explicit. No second serial client, bypass lock, live code injection or automatic service restart is justified by this diagnosis.

The next passive trace records only the existing hold; it cannot recover the earlier trajectory's transient. If a later, separately reviewed motion diagnostic is justified by that readback, record the pre-operation commanded goal and each generated setpoint as well as raw telemetry. That would test the predicted initial +0.77–0.90° command change without confusing a command change with physical motion. The offline reconstruction does not establish why the residual persisted 89 minutes later. At the saved final position, the requested target is 0.808791° away, inside the configured 2° relative-target threshold by 1.191209°; clipping is not predicted for that sample. This is arithmetic, not verification of the driver's actual second position read or raw hardware goal.

Constructing a new `LeRobotDriver` is not a passive read: connection configures torque and gains and sets the goal to the current position. Restarting the service would destroy the current commanded-versus-measured hold evidence and must be a separately coordinated maintenance action. The historical trace requires the service stopped; its independent lock is not permission to run alongside the service. It writes P, performs out/back steps and writes the start goal in cleanup, so it cannot serve as this passive diagnostic.

Interpret the passive result before proposing any further move:

| Observation | Next investigation |
| --- | --- |
| Raw goal differs from encoded command, or mode/P/I/D differ from expected configuration | Driver conversion, relative-target clamp, write/readback path and configuration provenance. Successful sync-write transmission alone is not a goal readback. |
| Raw goal matches but position remains offset with substantial current/load or voltage changes | Pose-dependent load/friction, power or mechanical resistance. Compare against a contemporaneous baseline; the old traces do not establish a universal fault threshold. |
| Raw goal matches but position remains offset with low current/load | Deadband, friction, servo configuration/firmware remain possible. Also compare the first trajectory setpoints with the previous command: the measured-start jump is a concrete timing/path hypothesis, not a proven physical cause. |
| Raw goal and measured position agree but the coordinator still reports a miss | Unit conversion, observation clock/age and operation snapshot semantics. |

Any later motion needs Kris's final camera confirmation and explicit release of the hold, fresh usable views, a clear raised workspace, healthy telemetry and a reviewed bounded diagnostic through the existing supervised executor. Retain P96, limits, the three-second lease, freshness/deadline checks and the 0.8 degree guard. Stop at the first failed settle; no automatic retry or return-to-start command. Preserve the last commanded hold and hand recovery to the operator. Do not touch the table. Above 60 C, defer motion for the handoff's ten-minute cooling pause and reassess. The 11:22 CEST supervision deadline does not itself release the hold.

## Commissioning prerequisites

The final camera framing must be confirmed and frozen. Resolve elbow tracking and establish a reachable raised home with repeatable measured returns before contact commissioning. Then measure the TCP with guarded first-contact stopping, touch distributed mat points, fit a nine-point workspace homography with held-out error under about 1 cm, and record/review the safe polygon and home. A failed settle at the current raised pose is not proof of table contact. Keep geometry-dependent live admission disabled until its configuration is reviewed. Three watched reset smokes and one watched trial per available model must pass before scored benchmark trials. Independent visual-operator software work can continue offline in the other agent's lane.

## Code and test provenance

At robo-harness `ac75f4b`: `python/robo_harness/engine.py:433` owns serialized ticks, measured completion and the failed-command hold; `python/robo_harness/drivers.py:35` configures torque/gains/goals on connection; `drivers.py:159` exposes position and temperature reads; `python/robo_harness/service.py:139` serves cached observation; `scripts/servo_step_trace.py` owns the historical active trace. The saved deployed bus source's `_sync_write` transmits the packet without a goal-register readback.

Offline reproduction: from robo-harness, `uv run --extra dev pytest var/elbow-diagnosis-2026-09-23/test_offline_diagnosis.py`. No hardware imports or device/network access are made by these tests. The evidence reducer derives predicted raw targets; it does not claim measured goal-register values.

Private evidence: `robo-harness/var/elbow-diagnosis-2026-09-23/`.
